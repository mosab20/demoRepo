pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                bat "gradle build"
                echo 'Building..'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Starting cloudformation job') {
            steps {
                script {
                    def foundFiles = bat(script: 'ls -1 cloud-formation', returnStdout: true).split()
                    foundFiles.each { file ->
                        build job: 'cloudformation', parameters: [
                            string(name: 'TEMPLATE_FILE', value: file),
                            string(name: 'ACTION', value: "create")
                        ]
                    }
                }
                echo 'Deploying....'
            }
        }
    }
}